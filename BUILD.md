# Сборка плагина для установки в Kodi

## 🤖 Автоматическая сборка (рекомендуется)

Проект настроен для автоматической сборки через GitHub Actions:

### Автоматические релизы

- **При push в main**: автоматически определяется тип релиза по коммитам
- **Ручной запуск**: через GitHub Actions с выбором типа релиза
- **Готовые ZIP файлы**: доступны на странице [Releases](https://github.com/ad/multiki/releases)

### Ручной запуск сборки

1. Перейдите в **Actions** → **Build and Release**
2. Нажмите **Run workflow**
3. Выберите тип релиза:
   - `patch`: исправления багов (1.0.0 → 1.0.1)
   - `minor`: новые функции (1.0.0 → 1.1.0)
   - `major`: критические изменения (1.0.0 → 2.0.0)
4. Нажмите **Run workflow**

## 🛠️ Локальная сборка

### Автоматическая сборка

```bash
# Создать ZIP файл для установки в Kodi
./build.sh
```

### Ручная сборка

1. **Создайте временную папку**:
   ```bash
   mkdir -p build/plugin.video.arjlover
   ```

2. **Скопируйте необходимые файлы**:
   ```bash
   cp addon.xml build/plugin.video.arjlover/
   cp main.py build/plugin.video.arjlover/
   cp -r resources build/plugin.video.arjlover/
   ```

3. **Создайте ZIP архив**:
   ```bash
   cd build
   zip -r plugin.video.arjlover-1.0.0.zip plugin.video.arjlover/
   ```

4. **Очистите временные файлы**:
   ```bash
   cd ..
   rm -rf build/plugin.video.arjlover
   ```

## Файлы, включаемые в установку

- `addon.xml` - метаданные плагина
- `main.py` - основной файл плагина
- `resources/lib/` - библиотеки плагина
  - `__init__.py`
  - `parser.py`
  - `cache.py`

## Файлы, НЕ включаемые в установку

- `tests/` - тесты разработчика
- `BUILD.md` - эта инструкция
- `README.md` - документация (опционально)
- `.git/` - история Git
- `__pycache__/` - кэш Python
- `.hypothesis/` - кэш тестов

## Проверка ZIP файла

Структура ZIP файла должна быть:

```
plugin.video.arjlover-1.0.0.zip
└── plugin.video.arjlover/
    ├── addon.xml
    ├── main.py
    └── resources/
        └── lib/
            ├── __init__.py
            ├── cache.py
            └── parser.py
```