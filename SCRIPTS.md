# Скрипты проекта

## 🔧 Основные скрипты

### `build.sh`
Создает ZIP файл для установки в Kodi
```bash
./build.sh
# Создает: plugin.video.arjlover-X.X.X.zip
```

### `version.py`
Управление версиями плагина
```bash
python3 version.py show                    # Показать текущую версию
python3 version.py set 1.2.3             # Установить версию
python3 version.py bump patch             # 1.0.0 → 1.0.1
python3 version.py bump minor             # 1.0.0 → 1.1.0
python3 version.py bump major             # 1.0.0 → 2.0.0
python3 version.py bump patch --tag       # + создать Git тег
```

### `check-release.sh`
Проверка готовности к релизу
```bash
./check-release.sh
# Проверяет: тесты, структуру, сборку
```

### `adb-install.sh`
Автоматическая установка плагина в Kodi через ADB
```bash
./adb-install.sh
# Автоматически находит Kodi, собирает и устанавливает плагин
```

### `install-to-kodi.sh`
Расширенная версия установки через ADB
```bash
./install-to-kodi.sh
# Более подробный вывод и дополнительные проверки
```

### `setup-git.sh`
Настройка Git для проекта
```bash
./setup-git.sh
# Настраивает шаблоны коммитов и другие параметры
```

## 🧪 Тестирование

### Запуск тестов
```bash
python3 -m pytest tests/ -v              # Все тесты
python3 -m pytest tests/ -k "round_trip" # Конкретные тесты
```

### Тесты с покрытием
```bash
pip install pytest-cov
python3 -m pytest tests/ --cov=resources.lib --cov-report=html
```

## 🚀 GitHub Actions

### `.github/workflows/test.yml`
- Запускается при каждом push/PR
- Тестирует на Python 3.8-3.11
- Проверяет структуру плагина

### `.github/workflows/release.yml`
- Ручной запуск через GitHub Actions
- Создает ZIP файлы и GitHub releases
- Семантическое версионирование

## 📋 Файлы конфигурации

### `.gitmessage`
Шаблон для коммитов с примерами Conventional Commits

### `.gitignore`
Исключает файлы разработки из Git и установки Kodi

## 🎯 Быстрые команды

```bash
# Полная проверка перед релизом
./check-release.sh

# Локальный релиз
python3 version.py bump minor
./build.sh

# Установка на Android TV
./adb-install.sh

# Настройка нового окружения
python3 -m venv venv
source venv/bin/activate
pip install pytest hypothesis
./setup-git.sh

# Тестирование
python3 -m pytest tests/ -v
```