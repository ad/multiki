# Руководство по участию в проекте

Спасибо за интерес к проекту Kodi ArjLover Plugin! 🎉

## 🚀 Быстрый старт

1. **Fork репозитория** и клонируйте свою копию:
   ```bash
   git clone https://github.com/your-username/multiki.git
   cd multiki
   ```

2. **Настройте окружение**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   pip install pytest hypothesis
   ```

3. **Настройте Git** (опционально):
   ```bash
   ./setup-git.sh
   ```

## 📝 Соглашения о коммитах

Мы используем [Conventional Commits](https://www.conventionalcommits.org/) для автоматического версионирования:

### Типы коммитов

- `feat:` - новая функциональность (minor релиз)
- `fix:` - исправление бага (patch релиз)
- `docs:` - изменения в документации (patch релиз)
- `style:` - форматирование кода (patch релиз)
- `refactor:` - рефакторинг без изменения функциональности (patch релиз)
- `test:` - добавление или изменение тестов (patch релиз)
- `chore:` - обновление зависимостей, настроек сборки (patch релиз)

### Критические изменения

- `BREAKING CHANGE:` в описании коммита (major релиз)
- `feat!:` или `fix!:` с восклицательным знаком (major релиз)

### Примеры

```bash
feat: добавить поддержку формата .webm
fix: исправить парсинг названий с апострофами
docs: обновить инструкцию по установке
refactor: упростить логику кэширования
test: добавить тесты для новых форматов

# Критические изменения
feat!: изменить структуру API парсера
BREAKING CHANGE: удалить поддержку старых версий Kodi
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
python -m pytest tests/ -v

# Конкретный тест
python -m pytest tests/test_parser.py::TestParser::test_decode_filename_basic_examples -v

# С покрытием кода
pip install pytest-cov
python -m pytest tests/ --cov=resources.lib --cov-report=html
```

### Добавление новых тестов

- **Unit тесты**: для конкретных примеров и граничных случаев
- **Property-based тесты**: для проверки свойств на случайных данных
- Все новые функции должны иметь тесты

## 🔄 Процесс разработки

### 1. Создание изменений

```bash
# Создайте ветку для функции
git checkout -b feat/new-video-format

# Внесите изменения
# Добавьте тесты
# Убедитесь, что тесты проходят
python -m pytest tests/ -v

# Зафиксируйте изменения
git add .
git commit -m "feat: добавить поддержку формата .webm"
```

### 2. Отправка Pull Request

1. Отправьте ветку в свой fork:
   ```bash
   git push origin feat/new-video-format
   ```

2. Создайте Pull Request на GitHub

3. Дождитесь прохождения CI/CD тестов

4. Ответьте на комментарии ревьюеров

### 3. Автоматические проверки

При создании PR автоматически запускаются:

- **Тесты** на Python 3.8-3.11
- **Проверка структуры** плагина
- **Валидация** addon.xml
- **Проверка импортов** модулей

## 📋 Чек-лист для PR

- [ ] Код соответствует стилю проекта
- [ ] Добавлены тесты для новой функциональности
- [ ] Все тесты проходят
- [ ] Обновлена документация (если необходимо)
- [ ] Коммит следует соглашениям
- [ ] PR имеет понятное описание

## 🐛 Сообщение о багах

При создании issue включите:

1. **Версию плагина** и Kodi
2. **Шаги для воспроизведения**
3. **Ожидаемое поведение**
4. **Фактическое поведение**
5. **Логи** (если есть)

## 💡 Предложение функций

Перед реализацией крупных изменений:

1. Создайте issue с описанием предложения
2. Дождитесь обсуждения и одобрения
3. Приступайте к реализации

## 📚 Полезные ссылки

- [Kodi Add-on Development](https://kodi.wiki/view/Add-on_development)
- [Python Style Guide](https://pep8.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Property-Based Testing](https://hypothesis.readthedocs.io/)

## 🤝 Код поведения

Мы придерживаемся дружелюбной и инклюзивной атмосферы. Пожалуйста:

- Будьте уважительны к другим участникам
- Конструктивно критикуйте код, а не людей
- Помогайте новичкам
- Используйте понятный русский или английский язык

---

Спасибо за ваш вклад в проект! 🙏