# Установка плагина через ADB

## 🔧 Настройка ADB

### 1. Установка ADB на компьютер

**macOS:**
```bash
brew install android-platform-tools
```

**Windows:**
- Скачайте [Platform Tools](https://developer.android.com/studio/releases/platform-tools)
- Распакуйте и добавьте в PATH

**Linux:**
```bash
sudo apt install android-tools-adb  # Ubuntu/Debian
sudo pacman -S android-tools        # Arch Linux
```

### 2. Настройка Android TV

1. **Включите режим разработчика:**
   - Настройки → Устройство → О программе
   - Нажмите на "Номер сборки" 7 раз
   - Появится "Вы стали разработчиком!"

2. **Включите USB отладку:**
   - Настройки → Устройство → Параметры разработчика
   - Включите "Отладка по USB"

3. **Включите отладку по сети (опционально):**
   - В "Параметры разработчика" включите "Отладка по сети"
   - Запомните IP адрес: Настройки → Сеть → IP адрес

## 📱 Подключение

### Через USB кабель (рекомендуется)

1. **Подключите Android TV к компьютеру** USB кабелем
2. **Разрешите отладку** на экране TV (появится запрос)
3. **Проверьте подключение:**
   ```bash
   adb devices
   ```

### Через Wi-Fi

1. **Подключитесь к тому же Wi-Fi** что и Android TV
2. **Подключитесь по IP:**
   ```bash
   adb connect 192.168.1.100:5555  # замените на IP вашего TV
   ```

## 🚀 Установка плагина

### Автоматическая установка

```bash
# Простая установка
./adb-install.sh

# Или полная версия
./install-to-kodi.sh
```

### Ручная установка

1. **Соберите плагин:**
   ```bash
   ./build.sh
   ```

2. **Скопируйте на устройство:**
   ```bash
   adb push plugin.video.arjlover-1.0.0.zip /sdcard/Download/
   ```

3. **Установите в Kodi:**
   ```bash
   # Найдите папку Kodi
   adb shell ls /storage/emulated/0/Android/data/org.xbmc.kodi/files/.kodi/addons/
   
   # Извлеките плагин
   adb shell "cd /storage/emulated/0/Android/data/org.xbmc.kodi/files/.kodi/addons && unzip -o /sdcard/Download/plugin.video.arjlover-1.0.0.zip"
   ```

## 🔍 Устранение неполадок

### ADB не видит устройство
- Проверьте USB кабель (должен поддерживать передачу данных)
- Перезапустите ADB: `adb kill-server && adb start-server`
- Попробуйте другой USB порт
- Убедитесь что драйверы установлены (Windows)

### Папка Kodi не найдена
- Запустите Kodi хотя бы один раз
- Проверьте альтернативные пути:
  ```bash
  adb shell find /storage -name ".kodi" 2>/dev/null
  ```

### Плагин не появляется в Kodi
- Перезапустите Kodi
- Проверьте что файлы скопировались:
  ```bash
  adb shell ls -la /storage/emulated/0/Android/data/org.xbmc.kodi/files/.kodi/addons/plugin.video.arjlover/
  ```
- Проверьте логи Kodi: Настройки → Система → Логирование

### Отладка по сети не работает
- Убедитесь что порт 5555 открыт
- Попробуйте подключиться через USB сначала
- Проверьте что устройства в одной сети

## 📋 Полезные команды ADB

```bash
# Список устройств
adb devices

# Подключение по сети
adb connect IP:5555

# Копирование файлов
adb push local_file /sdcard/
adb pull /sdcard/remote_file ./

# Выполнение команд
adb shell "ls -la /sdcard/"

# Установка APK
adb install app.apk

# Перезапуск ADB
adb kill-server && adb start-server

# Логи устройства
adb logcat
```

---

После установки плагин будет доступен в Kodi:
**Видео** → **Дополнения** → **ArjLover Cartoons**